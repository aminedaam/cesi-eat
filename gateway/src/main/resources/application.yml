server:
  port: 8090

spring:
  application:
    name: gateway
  cloud:
    gateway:
      mvc:
        routes:
          - id: route1
            uri: http://localhost:8081/
            predicates:
              - Path=/users/**