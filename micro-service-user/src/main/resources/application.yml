
spring:
  main:
    allow-circular-references: true
  cloud:
    config:
      discovery:
        enabled: false
      enabled: false
  application:
    name: user-service
  datasource:
    url: jdbc:sqlserver://172.16.10.12;databaseName=CESI_EATS;encrypt=false;
    username: sa
    password: CesiEat06


jpa:
  database-platform: org.hibernate.dialect.SQLServerDialect
  hibernate:
    ddl-auto: update
  show-sql: true

server:
  port: 4001
  servlet:
    context-path: /

jwt:
  secret: "AMINEALEXANDREGABRIELNADAAKRAMPROJECTCESI_EATS2025"